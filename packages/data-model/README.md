# packages/data-model

This package folder is part of the LegoLens Core v1.0 modular scaffold. The v1.0 bundle keeps the current runtime compatible while documenting the intended package boundaries for future refactoring.
