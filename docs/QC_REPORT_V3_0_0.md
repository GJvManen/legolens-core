# LegoLens Core v3.0.0 QC Report

## Scope

This report records the v3.0.0 local release validation scope for LegoLens Core.

## Validated areas

- v3.0.0 release metadata.
- v3 frontend entrypoint.
- Backend syntax and API surface.
- Static UI validation.
- Browser smoke validation.
- Candidate-only all-case ingestion.
- Runtime candidate persistence.
- Review state and share-approval separation.
- Legacy import support.
- Local export previews.
- Export safety for secret-like values.
- Audit event creation.
- Screenshot documentation policy.

## Commands

```bash
node --check app_v3.js
node --check compat.js
node --check backend/server.mjs
npm test
npm run browser:smoke
npm run release:check
npm run ingestion:run-all
npm run screenshots:capture
```

## Expected results

```text
PASS v3.0.0 validation
PASS UI static test
PASS v3.0.0 static browser/layout smoke
PASS v3.0.0 final release gate
PASS ingestion run-all framework test with candidate-only fallback
```

## Safety checks

- Candidate-only ingestion remains enforced.
- Review status does not imply share approval.
- Export output should exclude connector secrets.
- Backend connector fields remain references rather than frontend secrets.
- Runtime files are local operational state, not publication artifacts.

## Screenshot documentation check

The v3.0.0 repository uses reproducible real interface screenshot capture.

- Mockup SVG files have been removed from `docs/screenshots/v3_0/`.
- Obsolete v2 screenshot mockups have been removed from `docs/screenshots/v2_0/`.
- Real browser screenshots are generated with `scripts/capture_v3_screenshots.py`.
- Generated PNG files can be committed from a normal local Git client if binary release screenshots are required.

## Final gate summary

```json
{
  "release": "v3.0.0",
  "candidate_only_ingestion": true,
  "persistent_runtime": true,
  "review_share_separation": true,
  "legacy_import": true,
  "exports_tested": ["html", "csv", "geojson", "stix", "misp"],
  "screenshot_policy": "real browsercaptures generated by script"
}
```
